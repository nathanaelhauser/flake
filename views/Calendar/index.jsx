const React = require('react')
const Layout = require('../Layout')
const Navbar = require('../Navbar')

module.exports = props =>{
    return (
        <Layout>
            <Navbar/>
            </Layout>
    )
}