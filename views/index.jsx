const React = require('react')
// other variables with layout and props elements
const Layout = require('./Layout')

// export statement with layout and props elements

module.exports = props => {
  return (
    
    <Layout />

  )
}